ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [dev.remylavergne.spotfinder.ApplicationKt.module]
  }
}

mongodb {
  username = ${MONGO_USER}
  password = ${MONGO_PASS}
  hostname = ${MONGO_HOST}
  name = ${MONGO_DATABASE_NAME}
  port = ${MONGO_PORT}
}

pictures {
  upload {
    dir = "/pictures/"
  }
}